package com.bihe0832.readhub.libware.ui;import android.app.Activity;import android.view.View;import android.view.ViewGroup;import android.widget.TextView;import android.widget.Toast;import com.bihe0832.readhub.R;import com.bihe0832.readhub.libware.thread.ShakebaThreadManager;public class ToastUtil {    public static void show(final Activity activity,final String toastInfo,final int duration){        final View layout = activity.getLayoutInflater().inflate(R.layout.com_bihe0832_shakeba_common_toast,                (ViewGroup) activity.findViewById(R.id.custom_toast_layout_id));        // set a message        TextView toastText = (TextView) layout.findViewById(R.id.toastText);        toastText.setText(toastInfo);        // Toast...        ShakebaThreadManager.getInstance().runOnUIThread(new Runnable() {            @Override            public void run() {                Toast toast = new Toast(activity);                toast.setDuration(duration);                toast.setView(layout);                toast.show();            }        });    }  }  